import React from "react";
import "./style.css";

export default function Header() {
    return (
        <div className="headerCon">
            <h1>Our Groceries</h1>
            <p>Simply check the box if we need it. You can add more at the bottom of the list.</p>
        </div>
    )
}